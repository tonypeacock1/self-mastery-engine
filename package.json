{
  "name": "dianetics-engine",
  "version": "1.0.0",
  "private": true,
  "description": "Isolated Dianetics Engine: geo-EFT Worker + PSL static SILO builder. Branch target: feature/dianetics-engine",
  "type": "module",
  "scripts": {
    "build:example": "tsx scripts/build-example-site.ts",
    "typecheck": "tsc --noEmit",
    "worker:dev": "wrangler dev --config worker/wrangler.toml",
    "worker:deploy": "wrangler deploy --config worker/wrangler.toml",
    "build:wave1": "tsx scripts/build-wave1-site.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240919.0",
    "@types/node": "^26.2.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.2",
    "wrangler": "^3.78.0"
  }
}
